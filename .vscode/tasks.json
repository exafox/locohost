{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Remove report.html",
            "type": "shell",
            "command": "rm -f ${workspaceFolder}/report.html",
            "problemMatcher": []
        }
    ]
}
